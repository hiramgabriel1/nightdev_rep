services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: nightdev
      POSTGRES_PASSWORD: nightdev
      POSTGRES_DB: nightdev
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
